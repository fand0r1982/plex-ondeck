version: '3.8'
services:
  plex-ondeck:
    build: .  # Relativ zum Repo!
    ports:
      - "8088:80"
    environment:
      - PLEX_URL=http://localhost:32400
      - PLEX_TOKEN=DEIN_TOKEN
      - OPENAI_API_KEY=DEIN_KEY
    restart: unless-stopped